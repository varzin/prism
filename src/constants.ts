// The deploy base path (e.g. "/prism/") is handled by react-router's `basename`,
// so route paths and links are written root-relative. This prefix stays empty and
// is kept only so existing `${routePrefix}/...` link strings remain valid.
export const routePrefix = ''
